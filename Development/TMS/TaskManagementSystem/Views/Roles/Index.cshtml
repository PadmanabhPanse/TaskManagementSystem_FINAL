@using Microsoft.AspNetCore.Identity
@model IEnumerable<IdentityRole>
@{
    ViewData["Title"] = "Roles";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-header card-header-warning">
                <h4 class="card-title ">Roles</h4>
                <p class="card-category">List of Roles</p>
                <a class="btn btn-primary" asp-action="Create"><i class="material-icons">add</i></a>
                <a class="btn btn-primary" asp-action="accesscontrol">Access Control</a>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table">
                        <thead class=" text-primary">
                            <tr>
                                <th>
                                    ID
                                </th>
                                <th>
                                    Name
                                </th>
                                <th>
                                    Actions
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (Model != null && Model.Count() > 0)
                            {
                                foreach (var role in Model)
                                {
                                    <tr>
                                        <td>
                                            @role.Id
                                        </td>
                                        <td>
                                            @role.Name
                                        </td>
                                        <td class="text-primary">
                                            <a class="btn btn-link btn-primary" asp-action="edit" asp-route-id="@role.Id"><i class="material-icons">create</i></a>
                                            <a class="btn btn-link btn-danger" asp-action="delete" asp-route-id="@role.Id"><i class="material-icons">delete_sweep</i></a>
                                        </td>
                                    </tr>
                                }
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>